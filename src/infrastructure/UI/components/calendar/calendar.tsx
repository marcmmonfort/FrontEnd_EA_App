/*
import React from 'react';
import { View } from 'react-native';
import { Calendar, ICalendarEventBase } from 'react-native-big-calendar';
import { CalendarComponentProps } from './Types';



const CalendarComponent = <T extends ICalendarEventBase>({
  events,
  height,
  ...props
}: CalendarComponentProps<T>) => {
  /*return (
    <View >
      <Calendar<T> height={height} events={events} {...props} />
    </View>
  );
  */


//export default CalendarComponent;
